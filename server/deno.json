{
  "importMap": "./import_map.json",
  "lock": false,
  "exclude": [
    "dist/",
    "bundle/",
    "build/"
  ],
  "tasks": {
    "build": "deno run -A ./scripts/npm-build.ts",
    "build:server": "deno run -A ./scripts/bundle-server.ts --watch"
  }
}